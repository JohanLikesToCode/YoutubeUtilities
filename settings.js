// JavaScript is optional for now. You can add interactivity later if needed.
// For now, just add a placeholder script to handle the page.

document.addEventListener('DOMContentLoaded', function() {
    console.log("Settings page loaded.");
});
